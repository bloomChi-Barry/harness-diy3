grumphp:
    hooks_dir: ~
    hooks_preset: local
    tasks:
        phpcsfixer:
            config: .php-cs-fixer.dist.php
        phpstan:
            configuration: phpstan.neon
            ignore_patterns:
                - tests/
    parallel:
        enabled: true
    process_timeout: 60
